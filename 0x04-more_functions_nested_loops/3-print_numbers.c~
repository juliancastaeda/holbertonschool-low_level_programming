#include "holberton.h"

/**
 * _islower - print 1 or 0.
 * @c:evaluate if it is capital
 *
 * Return: Alaways 0 (Success)
 */

void print_numbers(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	{
	_putchar(n);
	}
	_putchar('\n');

	return (0);
}
