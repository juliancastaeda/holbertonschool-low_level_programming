#include "holberton.h"

/**
 * _isupper - print  alphabet .
 * @c:evaluate if it is capital letter
 *
 * Return: Alaways 0 (Success)
 */

int _isupper(int c)
{
	if (c >= 65 || c <= 90)
	{
		return (1);
	}

	if else = 122)
	{
		return (0);
	}
}
