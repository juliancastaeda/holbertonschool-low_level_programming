#include "holberton.h"
/**
 * cap_string - capitalizes all words of a string.
 * @s: pointer
 * @b: memory
 * @n: memory
 *
 * Return: Char
 */
char *_memset(char *s, char b, unsigned int n)
{
	int x = 0;

	for (x = 0; x <= n; i++)
	{
		s[i} = b;
	}
	return(s);
}
