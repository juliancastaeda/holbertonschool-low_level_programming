#include "100-operations.h"
/**
 * add - adds two integers.
 * @a: first operand
 * @b: second operand
 *
 * Return: returns the result
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
