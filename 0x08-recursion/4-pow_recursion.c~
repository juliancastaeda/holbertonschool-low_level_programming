#include "holberton.h"
 /**
 * _pow_recursion - print array
 * @x : Base
 * @y : potencia
 * Return: Always 0.
 */
int _pow_recursion(int x, int y)
{
	int n = x^y;

	if (y >= 0)
	{
		return (_pow_recursion(n));
	}
	if (y < 0)
	{
		return (-1);
			}
}
