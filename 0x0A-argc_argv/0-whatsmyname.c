#include <stdio.h>

/**
 * main - print the new name
 * @argc: arguments
 * @argv: array the arguments
 *
 * Return: always (0)
 **/

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
